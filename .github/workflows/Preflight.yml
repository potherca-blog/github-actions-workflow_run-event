---
name: Preflight

#on:
  #- pull_request
  #- push

on:
  pull_request:
    branches: [ main ]

jobs:
  preflight-job:
    name: Preflight Step
    runs-on: ubuntu-latest
    steps:
      - run: env
