---
name: Test

on:
  workflow_run:
    workflows:
      - Preflight
    types: 
      - completed
      - requested

jobs:
  test-job:
    name: Test Step
    runs-on: ubuntu-18.04
    steps:
      - uses: haya14busa/action-workflow_run-status@v1
      - uses: actions/checkout@v2
      - name: Debug
        shell: bash
        run: env
